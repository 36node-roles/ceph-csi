---
namespace: ceph
state: present
ingress_class: traefik
wait: no

rook_version: v1.7.1

ceph_state: "{{ state }}"
ceph_device: vdb

# chart values
ceph_chart_values: {}
ceph_cluster_chart_values: {}
